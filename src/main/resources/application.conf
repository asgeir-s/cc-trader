akka {
  loglevel = DEBUG
  actor {
    debug {
      receive = on
      lifecycle = off
    }
  }
}

postgres {
  host = "localhost"
  port = 5432
  dbname = "bitstamp"
  trader.dbname = "cctrader"
  driver = "org.postgresql.Driver"
  user = "asgeir"
  password = ""
  test.dbname = "bitcoinTEST"
}